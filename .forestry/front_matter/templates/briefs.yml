---
label: Briefs
hide_body: false
is_partial: false
fields:
- type: include
  template: post-meta-info
- type: select
  name: brief_type
  config:
    source:
      type: documents
  label: Brief Type
- type: include
  name: ''
  label: ''
  template: photo
- type: field_group
  name: details
  label: Details
  fields:
  - type: list
    config:
      use_select: true
      source:
        type: documents
  - type: list
    config:
      use_select: true
      source:
        type: documents
  - type: list
    config:
      use_select: true
      source:
        type: documents
  - type: list
    config:
      use_select: true
      source:
        type: documents
  - type: list
    config:
      use_select: true
      source:
        type: documents
- type: field_group_list
  name: downloads
  label: Downloads
  fields:
  - type: include
    template: download-media
